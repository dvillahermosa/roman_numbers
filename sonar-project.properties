# Organization and project keys are displayed in the right sidebar of the project homepage

sonar.projectKey=dvillahermosa_roman_numbers

# Uncomment the follow code to use sonarcloud.org
#sonar.organization=dvillahermosa
#sonar.host.url=https://sonarcloud.io
#sonar.login =b40c795bca09430573a40730a38bfc56dc515fcc

# Uncomment the follow code to use sonarqube, localhost
sonar.login =admin
sonar.password=admin

sonar.projectVersion=1.0
sonar.language=java
sonar.projectDescription=This is an example for the kata Roman numbers and sonarcloud
sonar.sources=src/main
sonar.tests=src/test
sonar.java.binaries=target/classes  
sonar.sourceEncoding=UTF-8
# Import tests execution reports (JUnit XML format).
# Set the property to the path of the directory containing all the XML reports.
sonar.junit.reportsPath=target/surefire-reports

  
# Import JaCoCo code coverage report.
# Set the property to the path of the JaCoCo .exec report.
#sonar.jacoco.reportPath=target/jacoco.exec <- DEPRECATED
sonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml
 
# --- optional properties ---
sonar.projectVersion=1.0-alpha

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=.
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8